---
name: ListSelect
route: /components/list-select
menu: 组件
---


import {Playground, Props} from "docz";
import ListSelect, {Item} from "./index";
import "antd/dist/antd.min.css";
import "../../dist/antd-extend-ui.css"

-# listSelect
## 属性
<Props of={ListSelect}/>

## 代码演示
<Playground>
  <ListSelect
    dataSource={[{id: '京', value: '北京'}, {id: '冀', value: '河北'}, {id: '粤', value: '广东'}, {id: '川', value: '四川'}]}
    renderItem={(item, index) => <Item style={{display: "block", padding: '12px 8px'}} value={item.id} key={index}>
      <div>{item.value}</div>
      <span style={{color: 'rgb(153, 153, 153)', marginTop: 4}}>{item.id}</span>
    </Item>}
  />
</Playground>
